version: 1.0

provider:
  name: openfaas
  gateway: http://openfaas.docker.localhost

functions:
  dummy-service:
    lang: dockerfile
    image: gperreymond/dummy-service:latest
    handler: domains/dummy
    annotations:
      com.openfaas.health.http.path: "/~node/health"
      com.openfaas.health.http.initialDelay: "30s"
